# vision_service 的依赖。故意跟 label_service 分开装：那边是纯 CPU 的 IMU 推理，
# 不该被 torch/SAM 这套 GPU 依赖拖着走。
fastapi>=0.110
uvicorn>=0.27
pydantic>=2
numpy>=1.24
# 注意：仓库根 requirements.txt 里是 opencv-python（带 GUI），两个装在同一个环境里
# 会互相覆盖 cv2 这个包。建议 vision_service 用独立 venv；非要共用的话就别装这行，
# 用根目录那个 opencv-python，功能上够用（这里只用到 imread/findContours/approxPolyDP）。
opencv-python-headless>=4.8
# SAM 2.1（Apache-2.0）。装法见 README：pip install git+https://github.com/facebookresearch/sam2.git
# torch 按机器上的 CUDA 版本单独装，不在这里钉版本——钉错了会把人家现成的环境搞坏
# 画面狗检测（第一步：这段视频里有没有狗）。只用 COCO 预训练的 dog 类，
# 不训练不微调。ultralytics 是 AGPL-3.0——内部自用不分发，不受影响；
# 哪天要把权重或服务对外分发，这一项要先换掉（RT-DETRv2 / D-FINE 是 Apache-2.0）。
# YOLO26 是 2026 年的那一代（ultralytics 仓库上确认过），太旧的 ultralytics
# 解析不出 yolo26n.pt 这个名字——报错里会提示 pip install -U ultralytics。
ultralytics>=8.3
# 「画面找片段」：抽帧问 Claude（视觉大模型走 API，不本地起模型）。没装/没配 key 时
# /api/v1/seek/status 如实说不可用，别的功能不受影响。
anthropic>=0.40
# OpenAI / 豆包 / Gemini 走 REST，只要 httpx
httpx>=0.25
# 画面向量索引（以图搜图 / 一句话搜）：SigLIP，只在本地跑。torch 同上不钉版本
transformers>=4.44
pillow>=10
# SigLIP 的分词器要它（文本搜索那一半），没装的话整个模型加载失败
sentencepiece>=0.1.99
protobuf>=3.20
# 姿态关键点（以图搜图的第二路信号）：RTMPose AP-10K 的 ONNX，rtmlib + onnxruntime 跑。
# 先装 CPU 版 onnxruntime（rtmpose-m 256 一帧 30ms 左右，建索引 1 fps 够用）；
# 想走 GPU 换成 onnxruntime-gpu（版本得跟 CUDA 对上，跟 torch 一样不在这里钉）
rtmlib>=0.0.13
onnxruntime>=1.16
# 夜视增强（可选）：Retinexformer 的网络结构文件要它。纯 python、几十 KB。
# basicsr 那一堆训练依赖（lmdb / tb_logger / 数据加载器）**不用装**——
# 结构文件是按路径单独加载的，训练框架的 __init__ 根本不会被执行
einops>=0.6
