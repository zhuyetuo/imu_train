# vision_service 的依赖。故意跟 label_service 分开装：那边是纯 CPU 的 IMU 推理，
# 不该被 torch/SAM 这套 GPU 依赖拖着走。
fastapi>=0.110
uvicorn>=0.27
pydantic>=2
numpy>=1.24
# 注意：仓库根 requirements.txt 里是 opencv-python（带 GUI），两个装在同一个环境里
# 会互相覆盖 cv2 这个包。建议 vision_service 用独立 venv；非要共用的话就别装这行，
# 用根目录那个 opencv-python，功能上够用（这里只用到 imread/findContours/approxPolyDP）。
opencv-python-headless>=4.8
# SAM 2.1（Apache-2.0）。装法见 README：pip install git+https://github.com/facebookresearch/sam2.git
# torch 按机器上的 CUDA 版本单独装，不在这里钉版本——钉错了会把人家现成的环境搞坏
# 画面狗检测（第一步：这段视频里有没有狗）。只用 COCO 预训练的 dog 类，
# 不训练不微调。ultralytics 是 AGPL-3.0——内部自用不分发，不受影响；
# 哪天要把权重或服务对外分发，这一项要先换掉（RT-DETRv2 / D-FINE 是 Apache-2.0）。
ultralytics>=8.0
